using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestuctionExplosion : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

   void Destruction()
	{
        Destroy(gameObject);
	}
}
